<!DOCTYPE html>
<html lang="vi">
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "AnKhangCRM" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="UTF-8">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Tailwind CSS - Using CDN for development (TODO: fix asset pipeline) %>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              brand: {
                blue: '#0B387A',
                red: '#D81620',
                orange: '#F37021',
                dark: '#333333',
                'blue-dark': '#082856',
                'blue-light': '#1a4a8f'
              }
            }
          }
        }
      }
    </script>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# Flatpickr CSS %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr@4.6.13/dist/flatpickr.min.css">

    <%# Font Awesome %>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">

    <%# TomSelect CSS %>
    <link href="https://cdn.jsdelivr.net/npm/tom-select@2.4.3/dist/css/tom-select.bootstrap5.min.css" rel="stylesheet">
    <style>
      /* TomSelect custom overrides for brand consistency - UI/UX ProMax compliant */
      .ts-wrapper { width: 100%; }
      .ts-control { 
        border: 1px solid #d1d5db !important; 
        border-radius: 0.5rem !important; 
        padding: 0.5rem 0.75rem !important;
        min-height: 42px !important;
        box-shadow: 0 1px 2px 0 rgb(0 0 0 / 0.05) !important;
        transition: border-color 200ms ease, box-shadow 200ms ease !important;
      }
      .ts-control:focus-within { 
        border-color: #0B387A !important; 
        box-shadow: 0 0 0 2px rgba(11, 56, 122, 0.2) !important;
      }
      .ts-dropdown { 
        border: 1px solid #e5e7eb !important; 
        border-radius: 0.5rem !important;
        box-shadow: 0 10px 15px -3px rgb(0 0 0 / 0.1) !important;
        margin-top: 4px !important;
        z-index: 9999 !important;
      }
      .ts-dropdown .option { 
        padding: 0.5rem 0.75rem !important; 
        cursor: pointer !important; 
        transition: background-color 150ms ease, color 150ms ease !important;
      }
      .ts-dropdown .option:hover, .ts-dropdown .active { 
        background-color: #0B387A !important; 
        color: white !important; 
      }
      .ts-wrapper.multi .ts-control > div { 
        background: #0B387A !important; 
        color: white !important; 
        border-radius: 0.375rem !important;
        padding: 0.25rem 0.5rem !important;
        margin: 2px !important;
        transition: opacity 150ms ease !important;
      }
      .ts-wrapper.multi .ts-control > div:hover { opacity: 0.9 !important; }
      .ts-wrapper.multi .ts-control > div .remove { 
        color: white !important; 
        cursor: pointer !important;
        transition: opacity 150ms ease !important;
      }
      .ts-wrapper.multi .ts-control > div .remove:hover { opacity: 0.7 !important; }
      .ts-wrapper .clear-button { color: #6b7280 !important; cursor: pointer !important; }
      /* Placeholder styling */
      .ts-control input::placeholder { color: #9ca3af !important; }
    </style>

    <%# TomSelect JS - load before importmap so it's available as global %>
    <script src="https://cdn.jsdelivr.net/npm/tom-select@2.4.3/dist/js/tom-select.complete.min.js"></script>

    <%# Hotwire %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 font-sans text-gray-900">
    <% if user_signed_in? %>
      <%# Authenticated layout with sidebar %>
      <div class="flex h-screen overflow-hidden">
        <%# Sidebar %>
        <%= render "shared/sidebar" %>

        <%# Main Content %>
        <main class="flex-1 flex flex-col min-w-0 overflow-hidden">
          <%# Header %>
          <%= render "shared/header" %>

          <%# Flash Messages %>
          <%= render "shared/flash_messages" %>

          <%# Custom Confirm Dialog %>
          <%= render "shared/confirm_dialog" %>

          <%# Scrollable Content %>
          <div class="flex-1 overflow-auto bg-gray-50 p-6 lg:p-8">
            <%= yield %>
          </div>
        </main>
      </div>
    <% else %>
      <%# Guest layout - no sidebar %>
      <div class="bg-white shadow">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex">
              <div class="flex-shrink-0 flex items-center">
                <span class="text-xl font-bold text-brand-blue">AnKhangCRM</span>
              </div>
            </div>
            <div class="flex items-center">
              <%= link_to "Sign in", new_user_session_path, class: "text-gray-500 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium" %>
              <%= link_to "Sign up", new_user_registration_path, class: "ml-4 px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-brand-blue hover:bg-brand-blue-dark shadow-sm" %>
            </div>
          </div>
        </div>
      </div>
      <%= render "shared/flash_messages" %>
      <%= yield %>
    <% end %>
  </body>
</html>
