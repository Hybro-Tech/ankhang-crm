<turbo-frame id="contact_list_frame">
<%# NOTE: turbo_stream_from subscription is in show.html.erb (outside turbo-frame) %>

<div class="overflow-x-auto">
  <table class="min-w-full divide-y divide-gray-100">
    <thead class="bg-gray-50 border-b border-gray-100">
      <tr>
        <th scope="col" class="pl-6 pr-3 py-4 text-left text-xs font-bold text-gray-500 uppercase tracking-wider w-1/3">Khách hàng</th>
        <th scope="col" class="px-3 py-4 text-left text-xs font-bold text-gray-500 uppercase tracking-wider">Nhu cầu</th>
        <th scope="col" class="px-3 py-4 text-left text-xs font-bold text-gray-500 uppercase tracking-wider">Trạng thái</th>
        <th scope="col" class="px-3 py-4 text-left text-xs font-bold text-gray-500 uppercase tracking-wider">Thời gian</th>
        <th scope="col" class="pl-3 pr-6 py-4 text-right text-xs font-bold text-gray-500 uppercase tracking-wider">Hành động</th>
      </tr>
    </thead>
    <tbody id="<%= tab %>_table_body" class="bg-white divide-y divide-gray-100">
      <% contacts.each do |contact| %>
        <tr id="contact_<%= contact.id %>" class="hover:bg-blue-50/50 transition-all duration-200 group relative">
          <!-- Customer Info - 2 Line Layout -->
          <td class="pl-6 pr-3 py-4 cursor-pointer"
              data-action="click->workspace-tabs#showPreview"
              data-preview-url="<%= sales_workspace_preview_path(contact) %>">
            <div class="flex items-start">
              <%# Avatar with gradient and source indicator %>
              <div class="relative flex-shrink-0">
                <div class="h-12 w-12 rounded-full flex items-center justify-center text-white font-bold text-lg shadow-md" style="background: linear-gradient(to bottom right, #3b82f6, #2563eb);">
                   <% if contact.name.present? %>
                      <%= contact.name.first.upcase %>
                   <% else %>
                      <i class="fa-solid fa-user"></i>
                   <% end %>
                </div>
                <%# Source indicator badge %>
                <% if contact.identity_source == 'zalo' || contact.zalo_id.present? || contact.zalo_qr.attached? %>
                  <div class="absolute -bottom-0.5 -right-0.5 h-5 w-5 bg-blue-500 rounded-full flex items-center justify-center text-white text-[9px] font-bold border-2 border-white shadow-sm" title="Zalo">
                    Z
                  </div>
                <% else %>
                  <div class="absolute -bottom-0.5 -right-0.5 h-5 w-5 bg-green-500 rounded-full flex items-center justify-center text-white text-[10px] border-2 border-white shadow-sm" title="Phone">
                    <i class="fa-solid fa-phone"></i>
                  </div>
                <% end %>
              </div>
              
              <%# Contact Details - 2 Lines %>
              <div class="ml-4 min-w-0 flex-1">
                <%# Line 1: Name + Code %>
                <div class="flex items-center space-x-2">
                  <span class="text-sm font-bold text-gray-900 group-hover:text-brand-blue transition-colors truncate"><%= contact.name %></span>
                  <span class="text-xs font-mono text-gray-400"><%= contact.code %></span>
                </div>
                
                <%# Line 2: Contact Info (Phone/Zalo) - Prominent %>
                <div class="mt-1.5 flex items-center space-x-3 text-sm">
                  <%# Phone - Always show if present %>
                  <% if contact.phone.present? %>
                    <div class="flex items-center space-x-1.5 bg-gray-50 rounded-md px-2 py-1">
                      <i class="fa-solid fa-phone text-green-500 text-xs"></i>
                      <span class="font-medium text-gray-700"><%= contact.phone %></span>
                      <button type="button"
                              data-controller="clipboard"
                              data-clipboard-text-value="<%= contact.phone %>"
                              data-action="click->clipboard#copy"
                              class="text-gray-400 hover:text-gray-600 p-0.5"
                              title="Copy SĐT"
                              onclick="event.stopPropagation();">
                        <i class="fa-solid fa-copy text-xs"></i>
                      </button>
                    </div>
                  <% end %>
                  
                  <%# Zalo ID - Show if present %>
                  <% if contact.zalo_id.present? %>
                    <div class="flex items-center space-x-1.5 bg-blue-50 rounded-md px-2 py-1">
                      <span class="font-bold text-blue-600 text-xs">Z</span>
                      <span class="font-medium text-blue-700"><%= contact.zalo_id %></span>
                      <button type="button"
                              data-controller="clipboard"
                              data-clipboard-text-value="<%= contact.zalo_id %>"
                              data-action="click->clipboard#copy"
                              class="text-blue-400 hover:text-blue-600 p-0.5"
                              title="Copy Zalo ID"
                              onclick="event.stopPropagation();">
                        <i class="fa-solid fa-copy text-xs"></i>
                      </button>
                    </div>
                  <% end %>
                  
                  <%# QR Code - Show inline for quick scan %>
                  <% if contact.zalo_qr.attached? %>
                    <div class="relative group/qr" onclick="event.stopPropagation();">
                      <%# Small QR for quick scan %>
                      <div class="w-10 h-10 bg-white rounded-lg border border-gray-200 p-0.5 shadow-sm hover:shadow-md hover:border-blue-300 transition-all cursor-pointer">
                        <%= image_tag qr_code_contact_path(contact), class: "w-full h-full object-contain", alt: "QR Zalo" %>
                      </div>
                      <%# Hover popup - larger QR %>
                      <div class="hidden group-hover/qr:block absolute left-0 bottom-full mb-2 z-50">
                        <div class="bg-white rounded-xl shadow-2xl border border-gray-200 p-3">
                          <div class="text-center text-xs font-medium text-gray-600 mb-2">Quét để liên hệ Zalo</div>
                          <%= image_tag qr_code_contact_path(contact), class: "w-32 h-32 object-contain", alt: "QR Zalo" %>
                        </div>
                      </div>
                    </div>
                  <% end %>
                </div>
              </div>
            </div>
          </td>

          <!-- Service Type -->
          <td class="px-3 py-4 whitespace-nowrap cursor-pointer"
              data-action="click->workspace-tabs#showPreview"
              data-preview-url="<%= sales_workspace_preview_path(contact) %>">
            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-purple-50 text-purple-700 border border-purple-100">
              <%= contact.service_type&.name || "Chưa xác định" %>
            </span>
          </td>

          <!-- Status -->
          <td class="px-3 py-4 whitespace-nowrap cursor-pointer"
              data-action="click->workspace-tabs#showPreview"
              data-preview-url="<%= sales_workspace_preview_path(contact) %>">
            <%# TASK-064: Simplified to 4 statuses %>
            <% status_colors = {
              'new_contact' => 'bg-blue-50 text-blue-700 border-blue-100',
              'potential' => 'bg-teal-50 text-teal-700 border-teal-100',
              'failed' => 'bg-red-50 text-red-700 border-red-100',
              'closed' => 'bg-green-50 text-green-700 border-green-100'
            } %>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border <%= status_colors[contact.status] || 'bg-gray-50 text-gray-700 border-gray-100' %>">
              <%= contact.status_label %>
            </span>
          </td>

          <!-- Time -->
          <td class="px-3 py-4 whitespace-nowrap text-sm text-gray-400 cursor-pointer"
              data-action="click->workspace-tabs#showPreview"
              data-preview-url="<%= sales_workspace_preview_path(contact) %>">
            <div class="flex items-center">
              <i class="fa-regular fa-clock mr-1.5 text-gray-300"></i>
              <% if tab == :new_contacts %>
                <span><%= format_short_time(contact.created_at) %></span>
              <% else %>
                <span><%= format_short_time(contact.updated_at) %></span>
              <% end %>
            </div>
          </td>

          <!-- Actions -->
          <td class="pl-3 pr-6 py-4 whitespace-nowrap text-right text-sm font-medium">
            <div class="flex items-center justify-end space-x-3">
              <% if tab == :new_contacts %>
                <!-- Highlight 'Pick' Button (High Contrast & Reliable Rendering) -->
                <%= button_to pick_contact_path(contact), method: :post,
                    form: { class: "inline-block", data: { turbo_confirm: "Nhận khách hàng #{contact.name}?", turbo_frame: "_top" } },
                    class: "inline-flex items-center px-6 py-2 bg-[#0B387A] hover:bg-[#1a4b9c] text-white text-sm font-bold rounded-full shadow-[0_4px_14px_0_rgba(11,56,122,0.39)] hover:shadow-[0_6px_20px_rgba(11,56,122,0.23)] hover:-translate-y-1 transition-all duration-300 ease-out focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#0B387A] uppercase tracking-wider group-hover/btn:scale-105" do %>
                  <i class="fa-solid fa-dollar-sign mr-2 animate-bounce"></i>NHẬN
                <% end %>
              <% else %>
                <%# Detail Button - Go to Contact Detail to add interactions %>
                <%= link_to contact_path(contact), 
                    data: { turbo_frame: "_top" },
                    class: "inline-flex items-center px-3 py-1.5 bg-brand-blue text-white text-xs font-medium rounded-md hover:bg-blue-800 transition-colors shadow-sm" do %>
                  <i class="fa-solid fa-eye mr-1.5"></i>Chi tiết
                <% end %>
                <%# Standard Update Button %>
                <%= link_to edit_contact_path(contact), 
                    data: { turbo_frame: "_top" },
                    class: "inline-flex items-center px-3 py-1.5 bg-white border border-gray-300 text-gray-700 text-xs font-medium rounded-md hover:bg-gray-50 transition-colors shadow-sm" do %>
                  <i class="fa-solid fa-pen mr-1.5 text-gray-500"></i>Sửa
                <% end %>
              <% end %>
            </div>
          </td>
        </tr>
      <% end %>
      <% if contacts.empty? %>
        <tr>
          <td colspan="5" class="px-6 py-16 text-center">
            <div class="text-gray-400">
              <div class="bg-gray-50 h-24 w-24 rounded-full flex items-center justify-center mx-auto mb-4">
                <i class="fa-solid fa-inbox text-4xl text-gray-300"></i>
              </div>
              <p class="text-base font-medium text-gray-900 mb-1">
                <% case tab %>
                <% when :new_contacts %>
                  Không có khách hàng mới
                <% when :needs_update %>
                  Tuyệt vời! Bạn đã cập nhật hết thông tin
                <% when :potential %>
                  Chưa có khách hàng đang chăm sóc
                <% end %>
              </p>
              <p class="text-sm text-gray-500">
                <% case tab %>
                <% when :new_contacts %>
                  Hiện tại chưa có khách hàng mới trong khu vực của bạn.
                <% when :needs_update %>
                  Hãy kiểm tra lại sau nhé.
                <% when :potential %>
                  Hãy nhận khách hàng mới để bắt đầu.
                <% end %>
              </p>
            </div>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
</turbo-frame>
