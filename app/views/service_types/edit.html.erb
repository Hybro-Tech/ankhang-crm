<%# TASK-019: Edit Service Type %>
<% content_for :page_title, "Sửa loại nhu cầu" %>

<div class="max-w-2xl mx-auto">
  <%# Header %>
  <div class="mb-6">
    <nav class="flex items-center gap-2 text-sm text-gray-500 mb-4">
      <%= link_to service_types_path, class: "hover:text-brand-blue" do %>
        <i class="fa-solid fa-layer-group mr-1"></i>Loại nhu cầu
      <% end %>
      <i class="fa-solid fa-chevron-right text-xs"></i>
      <span class="text-gray-900">Sửa: <%= @service_type.name %></span>
    </nav>
    <h1 class="text-2xl font-bold text-gray-900">Sửa loại nhu cầu</h1>
    <p class="text-sm text-gray-500 mt-1">Cập nhật thông tin loại nhu cầu "<%= @service_type.name %>"</p>
  </div>

  <%# Form Card %>
  <div class="bg-white rounded-xl shadow-sm border border-gray-100 p-6">
    <%= render "form" %>
  </div>
</div>
