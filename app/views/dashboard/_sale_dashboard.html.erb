<div class="min-h-screen bg-white flex flex-col">
  <!-- Header -->
  <div class="sm:flex sm:items-center sm:justify-between mb-8">
      <div>
          <h1 class="text-2xl font-bold text-gray-900">Tổng quan</h1>
          <p class="mt-1 text-sm text-gray-500">Hiệu quả kinh doanh và công việc cần làm.</p>
      </div>
      <div class="mt-4 sm:mt-0 flex space-x-3">
          <span class="inline-flex rounded-md shadow-sm">
              <button type="button" class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none">
                  <i class="fa-solid fa-calendar mr-2 text-gray-400"></i> Tháng này
              </button>
          </span>
      </div>
  </div>

  <!-- KPI Cards -->
  <div class="grid grid-cols-1 gap-5 sm:grid-cols-2 lg:grid-cols-4 mb-8">
      <!-- Total Contacts -->
      <div class="bg-white overflow-hidden shadow-sm rounded-xl border border-gray-100 hover:shadow-md transition-shadow">
          <div class="p-5">
              <div class="flex items-center">
                  <div class="flex-shrink-0 bg-blue-50 rounded-md p-3">
                      <i class="fa-solid fa-users text-brand-blue text-xl"></i>
                  </div>
                  <div class="ml-5 w-0 flex-1">
                      <dl>
                          <dt class="text-sm font-medium text-gray-500 truncate">Tổng khách hàng</dt>
                          <dd>
                              <div class="text-2xl font-bold text-gray-900"><%= @kpi[:total_contacts] %></div>
                          </dd>
                      </dl>
                  </div>
              </div>
          </div>
      </div>

      <!-- New Leads -->
      <div class="bg-white overflow-hidden shadow-sm rounded-xl border border-gray-100 hover:shadow-md transition-shadow">
          <div class="p-5">
              <div class="flex items-center">
                  <div class="flex-shrink-0 bg-orange-50 rounded-md p-3">
                      <i class="fa-solid fa-user-plus text-brand-orange text-xl"></i>
                  </div>
                  <div class="ml-5 w-0 flex-1">
                      <dl>
                          <dt class="text-sm font-medium text-gray-500 truncate">Khách tiềm năng mới</dt>
                          <dd>
                              <div class="text-2xl font-bold text-gray-900"><%= @kpi[:new_leads] %></div>
                          </dd>
                      </dl>
                  </div>
              </div>
          </div>
      </div>

      <!-- Won Deals -->
      <div class="bg-white overflow-hidden shadow-sm rounded-xl border border-gray-100 hover:shadow-md transition-shadow">
          <div class="p-5">
              <div class="flex items-center">
                  <div class="flex-shrink-0 bg-green-50 rounded-md p-3">
                      <i class="fa-solid fa-check-circle text-green-600 text-xl"></i>
                  </div>
                  <div class="ml-5 w-0 flex-1">
                      <dl>
                          <dt class="text-sm font-medium text-gray-500 truncate">Đơn chốt thành công</dt>
                          <dd>
                              <div class="text-2xl font-bold text-gray-900"><%= @kpi[:won_deals_count] %></div>
                          </dd>
                      </dl>
                  </div>
              </div>
          </div>
      </div>

      <!-- Revenue (Mock) -->
      <div class="bg-white overflow-hidden shadow-sm rounded-xl border border-gray-100 hover:shadow-md transition-shadow">
          <div class="p-5">
              <div class="flex items-center">
                  <div class="flex-shrink-0 bg-red-50 rounded-md p-3">
                      <i class="fa-solid fa-sack-dollar text-brand-red text-xl"></i>
                  </div>
                  <div class="ml-5 w-0 flex-1">
                      <dl>
                          <dt class="text-sm font-medium text-gray-500 truncate">Doanh thu (Ước tính)</dt>
                          <dd>
                              <div class="text-2xl font-bold text-gray-900"><%= number_to_currency(@kpi[:revenue], unit: "₫", format: "%n %u", precision: 0) %></div>
                          </dd>
                      </dl>
                  </div>
              </div>
          </div>
      </div>
  </div>

  <!-- Charts Section -->
  <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-8">
      <!-- Main Chart -->
      <div class="bg-white shadow-sm rounded-xl border border-gray-100 p-6 lg:col-span-2">
          <h3 class="text-lg leading-6 font-semibold text-gray-900 mb-4">Biểu đồ hiệu quả kinh doanh</h3>
          <div class="relative h-72 w-full">
              <canvas id="mainChart"></canvas>
          </div>
      </div>

      <!-- Secondary Chart (Pie) -->
      <div class="bg-white shadow-sm rounded-xl border border-gray-100 p-6">
          <h3 class="text-lg leading-6 font-semibold text-gray-900 mb-4">Trạng thái khách hàng</h3>
          <div class="relative h-64 w-full flex justify-center">
              <canvas id="pieChart"></canvas>
          </div>
      </div>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-8">
      <!-- Recent Contacts List (To Process) -->
      <div class="bg-white shadow-sm rounded-xl border border-gray-100 lg:col-span-2 overflow-hidden flex flex-col">
          <div class="px-6 py-5 border-b border-gray-100 flex justify-between items-center">
              <h3 class="text-lg leading-6 font-semibold text-gray-900">🔔 Cần xử lý ngay</h3>
              <%= link_to "Xem tất cả", contacts_path, class: "text-sm text-brand-blue hover:text-blue-800 font-medium" %>
          </div>
          <div class="overflow-x-auto">
              <table class="min-w-full divide-y divide-gray-200">
                  <thead class="bg-gray-50">
                      <tr>
                          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Khách hàng</th>
                          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Trạng thái</th>
                          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Nhu cầu</th>
                          <th scope="col" class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Hành động</th>
                      </tr>
                  </thead>
                  <tbody class="bg-white divide-y divide-gray-200">
                      <% @sales_contacts.each do |contact| %>
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center">
                                    <div class="flex-shrink-0 h-8 w-8 bg-blue-100 rounded-full flex items-center justify-center text-brand-blue font-bold text-xs">
                                        <%= contact.name.first.upcase %>
                                    </div>
                                    <div class="ml-4">
                                        <div class="text-sm font-medium text-gray-900"><%= contact.name %></div>
                                        <div class="text-xs text-gray-500"><%= contact.code %></div>
                                    </div>
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full 
                                    <%= contact.status_label == 'Mới' ? 'bg-blue-100 text-blue-800' : 'bg-yellow-100 text-yellow-800' %>">
                                    <%= contact.status_label %>
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                <%= contact.service_type&.name %>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                <%= link_to "Gọi ngay", contact_path(contact), class: "text-brand-blue hover:text-blue-900 bg-blue-50 hover:bg-blue-100 px-3 py-1 rounded-md transition-colors" %>
                            </td>
                        </tr>
                      <% end %>
                      <% if @sales_contacts.empty? %>
                        <tr>
                            <td colspan="4" class="px-6 py-12 text-center text-gray-500">
                                Chưa có khách hàng nào cần xử lý.
                            </td>
                        </tr>
                      <% end %>
                  </tbody>
              </table>
          </div>
      </div>

      <!-- Upcoming Appointments & Activities -->
      <div class="space-y-8">
          <!-- Appointments -->
          <div class="bg-white shadow-sm rounded-xl border border-gray-100 overflow-hidden">
              <div class="px-6 py-5 border-b border-gray-100">
                  <h3 class="text-lg leading-6 font-semibold text-gray-900">📅 Lịch hẹn sắp tới</h3>
              </div>
              <ul class="divide-y divide-gray-200">
                  <% @upcoming_appointments.each do |appointment| %>
                    <li class="p-4 hover:bg-gray-50 transition-colors">
                        <div class="flex space-x-3">
                            <div class="flex-shrink-0 h-10 w-10 rounded-lg bg-red-50 flex flex-col items-center justify-center text-brand-red border border-red-100">
                                <span class="text-xs font-bold"><%= appointment.scheduled_at.strftime("%d") %></span>
                                <span class="text-[10px] uppercase"><%= appointment.scheduled_at.strftime("%b") %></span>
                            </div>
                            <div class="flex-1 space-y-1">
                                <div class="flex items-center justify-between">
                                    <h3 class="text-sm font-medium text-gray-900"><%= appointment.contact.name %></h3>
                                    <span class="text-xs text-gray-500"><%= appointment.scheduled_at.strftime("%H:%M") %></span>
                                </div>
                                <p class="text-xs text-gray-500">
                                    <%= appointment.scheduled_at.strftime("%H:%M") %> - <%= appointment.contact.service_type&.name %>
                                </p>
                                <% if appointment.content.present? %>
                                    <p class="text-xs text-gray-400 truncate"><i class="fa-regular fa-note-sticky mr-1"></i><%= appointment.content %></p>
                                <% end %>
                            </div>
                        </div>
                    </li>
                  <% end %>
                  <% if @upcoming_appointments.empty? %>
                    <li class="p-6 text-center text-sm text-gray-500">Không có lịch hẹn nào sắp tới.</li>
                  <% end %>
              </ul>
          </div>
      </div>
  </div>

  <!-- Leaderboard & Recent Activities -->
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-8">
      <!-- Leaderboard -->
      <div class="bg-white shadow-sm rounded-xl border border-gray-100 overflow-hidden">
          <div class="px-6 py-5 border-b border-gray-100 flex justify-between items-center">
              <h3 class="text-lg leading-6 font-semibold text-gray-900">🏆 Top Nhân viên xuất sắc</h3>
              <a href="#" class="text-sm text-brand-blue hover:text-blue-800 font-medium">Xem tất cả</a>
          </div>
          <div class="overflow-x-auto">
              <table class="min-w-full divide-y divide-gray-200">
                  <thead class="bg-gray-50">
                      <tr>
                          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Họ tên</th>
                          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Đơn hàng</th>
                          <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Doanh thu</th>
                      </tr>
                  </thead>
                  <tbody class="bg-white divide-y divide-gray-200">
                      <% @top_performers.each_with_index do |performer, index| %>
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <div class="flex items-center">
                                    <div class="flex-shrink-0 h-8 w-8">
                                        <img class="h-8 w-8 rounded-full" src="<%= performer[:avatar] %>" alt="User Avatar">
                                    </div>
                                    <div class="ml-4">
                                        <div class="text-sm font-medium text-gray-900"><%= performer[:name] %></div>
                                    </div>
                                </div>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                                    <%= performer[:deals] %>
                                </span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                <%= number_to_currency(performer[:revenue], unit: "₫", format: "%n %u", precision: 0) %>
                            </td>
                        </tr>
                      <% end %>
                  </tbody>
              </table>
          </div>
      </div>

       <!-- Recent Activities (Mock) -->
      <div class="bg-white shadow-sm rounded-xl border border-gray-100 overflow-hidden">
          <div class="px-6 py-5 border-b border-gray-100">
              <h3 class="text-lg leading-6 font-semibold text-gray-900">🔔 Hoạt động hệ thống</h3>
          </div>
          <ul class="divide-y divide-gray-200">
              <% @recent_activities.each do |activity| %>
                <li class="p-4 hover:bg-gray-50 transition-colors">
                    <div class="flex space-x-3">
                        <div class="mt-1">
                            <i class="<%= activity[:icon] %> <%= activity[:color] %>"></i>
                        </div>
                        <div class="ml-3 flex-1">
                            <p class="text-sm text-gray-900"><%= activity[:text] %></p>
                            <p class="text-xs text-gray-400 mt-1"><%= activity[:time] %></p>
                        </div>
                    </div>
                </li>
              <% end %>
          </ul>
      </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
    document.addEventListener("turbo:load", function() {
        // Main Chart (Line)
        const ctxMain = document.getElementById('mainChart');
        if (ctxMain) {
            new Chart(ctxMain.getContext('2d'), {
                type: 'line',
                data: {
                    labels: <%= raw @chart_data[:labels].to_json %>,
                    datasets: [{
                        label: 'Khách hàng',
                        data: <%= raw @chart_data[:contacts].to_json %>,
                        borderColor: '#0B387A',
                        tension: 0.4,
                        fill: false
                    }, {
                        label: 'Chốt đơn',
                        data: <%= raw @chart_data[:deals].to_json %>,
                        borderColor: '#10B981',
                        tension: 0.4,
                        fill: false
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: { legend: { position: 'top' } },
                    scales: {
                        y: { beginAtZero: true, grid: { borderDash: [2, 4] } },
                        x: { grid: { display: false } }
                    }
                }
            });
        }

        // Pie Chart
        const ctxPie = document.getElementById('pieChart');
        if (ctxPie) {
            new Chart(ctxPie.getContext('2d'), {
                type: 'doughnut',
                data: {
                    labels: ['Mới', 'Tiềm năng', 'Chốt', 'Huỷ'],
                    datasets: [{
                        data: [35, 45, 15, 5], // Mock distribution
                        backgroundColor: ['#0B387A', '#FBBF24', '#10B981', '#EF4444'],
                        borderWidth: 0
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: { legend: { display: false } },
                    cutout: '70%'
                }
            });
        }
    });
</script>
