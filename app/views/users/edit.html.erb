<% content_for :title, "Sửa nhân viên - #{@user.name}" %>

<% content_for :title, "Sửa nhân viên - #{@user.name}" %>

<div class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
  <h1 class="text-2xl font-bold text-gray-900 mb-6">Sửa Nhân viên</h1>
  <%= render "form", user: @user %>
</div>
