<%# TASK-RBAC: New Permission Page - matching Roles UI pattern %>
<% content_for :title, "Thêm Permission" %>

<%# Page Title %>
<div class="flex items-center mb-6">
  <%= link_to permissions_path, class: "mr-4 text-gray-500 hover:text-brand-blue transition-colors" do %>
    <i class="fa-solid fa-arrow-left text-lg"></i>
  <% end %>
  <div>
    <h1 class="text-2xl font-bold text-gray-900">Thêm Permission mới</h1>
    <p class="text-sm text-gray-500 mt-1">Tạo quyền mới cho hệ thống</p>
  </div>
</div>

<%= render "form", permission: @permission %>
