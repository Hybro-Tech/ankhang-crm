<h2>Yêu cầu chuyển giao bị từ chối</h2>

<p>
  <span class="badge badge-danger">TỪ CHỐI</span>
</p>

<p>Xin chào <%= @requester.name %>,</p>

<p>Yêu cầu chuyển giao khách hàng của bạn đã bị Team Lead từ chối.</p>

<div class="info-box">
  <div class="label">Khách hàng</div>
  <div class="value"><strong><%= @contact.name %></strong></div>
</div>

<div class="info-box">
  <div class="label">Loại yêu cầu</div>
  <div class="value"><%= @request.request_type_label %></div>
</div>

<div class="info-box">
  <div class="label">Từ</div>
  <div class="value"><%= @request.from_user.name %></div>
</div>

<% if @request.to_user.present? %>
<div class="info-box">
  <div class="label">Đến</div>
  <div class="value"><%= @request.to_user.name %></div>
</div>
<% end %>

<div class="info-box">
  <div class="label">Người từ chối</div>
  <div class="value"><%= @request.approved_by&.name %></div>
</div>

<% if @request.rejection_reason.present? %>
<div class="info-box" style="border-left-color: #ef4444;">
  <div class="label">Lý do từ chối</div>
  <div class="value"><%= @request.rejection_reason %></div>
</div>
<% end %>

<p style="text-align: center;">
  <a href="<%= @action_url %>" class="btn-primary">Quay lại danh sách</a>
</p>
