<%# TASK-REGION: Edit region page %>
<div class="container mx-auto px-4 py-6">
  <div class="max-w-2xl mx-auto">
    <div class="mb-6">
      <h1 class="text-2xl font-bold text-gray-900"><%= t("regions.edit") %></h1>
      <p class="text-sm text-gray-500 mt-1">Chỉnh sửa vùng/miền: <strong><%= @region.name %></strong></p>
    </div>

    <div class="bg-white shadow sm:rounded-lg p-6">
      <%= render "form" %>
    </div>
  </div>
</div>
